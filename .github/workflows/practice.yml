name: Practice
# run-name: ${{ github.event.head_commit.message }} by @${{ github.actor }}
on:
  push:
  #   paths:
  #     - '**.js'
  #   branches:
  #   tags:
  workflow_dispatch:
  # schedule:
    # - cron: '0/10 * * * *' # запуск каждые 10 минут
    # - cron: '55 6 9 2 MON' # запуск в понедельник 2 февраля, 6:55 UTC time
jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every time
        run: |
          echo "🕒 Current UTC time: $(date -u)"